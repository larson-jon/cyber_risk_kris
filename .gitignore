# Environment / secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Collected data output (ignore contents, but not the folder itself, so the
# cached NVD monthly totals below can be re-included).
data/*
# ...but keep the small cached NVD monthly totals (report reference data).
!data/nvd_monthly_totals.json
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
